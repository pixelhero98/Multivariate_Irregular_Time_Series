from Dataset.bms_air_dataset import BMSCacheConfig, prepare_bms_air_cache

cfg = BMSCacheConfig(
    data_dir="./bms_air_cache",          # where the cache should live
    raw_data_dir="./bms_air_quality_data",
    window=336,                          # must cover the max K you’ll train with
    horizon=168,                         # must cover the max H you’ll train with
)
prepare_bms_air_cache(cfg)
