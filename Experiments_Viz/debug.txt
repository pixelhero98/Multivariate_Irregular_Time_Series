cfg = ISDCacheConfig(
    years=[2021, 2022],          # required
    country="UK",                # default is "UK"
    max_stations=5,              # keep it small to start
    raw_data_dir="./nnoa_isd_raw",   # optional: caches raw Parquet per station-year
    data_dir="./nnoa_isd_cache",     # where the compact cache is written
    min_coverage=0.80,               # relax if stations get filtered out
    overwrite=False
)

result = prepare_isd_cache(cfg)
